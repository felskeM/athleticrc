---
interface Props {
  href?: string;
  variant?: "primary" | "secondary";
}

const { href, variant = "primary" } = Astro.props;

const classes =
  variant === "primary"
    ? "inline-flex items-center justify-center rounded-full bg-[var(--arc-blue)] px-5 py-3 text-sm font-semibold text-white transition hover:bg-[var(--arc-accent-2)]"
    : "inline-flex items-center justify-center rounded-full border border-white/15 bg-white/5 px-5 py-3 text-sm font-semibold text-white transition hover:bg-white/10";
---

{href ? (
  <a href={href} class={classes}>
    <slot />
  </a>
) : (
  <button class={classes}>
    <slot />
  </button>
)}