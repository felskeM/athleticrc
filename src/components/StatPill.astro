---
interface Props {
  label: string;
  value: string;
}

const { label, value } = Astro.props;
---

<div class="panel-shell p-4">
  <p class="text-xs uppercase tracking-[0.18em] text-[var(--arc-muted)]">{label}</p>
  <p class="mt-2 text-lg font-semibold text-[var(--arc-white)]">{value}</p>
</div>